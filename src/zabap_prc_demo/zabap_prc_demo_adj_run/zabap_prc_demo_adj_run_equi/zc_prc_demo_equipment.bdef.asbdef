projection;
strict ( 2 );
use draft;

define behavior for ZC_PRC_DEMO_Equipment alias Equipment
{
  use create;
  use update;
  use delete;

  use action copy_by_reference;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;
}